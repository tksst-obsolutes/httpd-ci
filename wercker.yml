box: tksst/httpd-build-test-centos6:wercker-docker-test
build:
  steps:
    - script:
        name: build
        code: |
          chown -R testuser . && su testuser sh -c "build/build.sh && build/test.sh"
